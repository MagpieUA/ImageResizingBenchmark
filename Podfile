# Uncomment the next line to define a global platform for your project
 platform :ios, '11.0'

target 'ImageResizingBenchmark' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageResizingBenchmark
  pod 'Kingfisher', '~> 5.0'
  pod 'SDWebImage', '~> 4.0'
  pod 'PINRemoteImage'
  pod 'Nuke'

  target 'ImageResizingBenchmarkTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
